# 多 Agent 长期协作规则

## 角色分工

- **Sol**：仅负责方案设计、关键决策和最终审查；不负责代码实现、修改或测试执行。
- **Terra**：负责代码实现、修改和测试验证。
- **Luna**：若可用，负责资料检索与代码探索；若不可用，改由低推理强度的 Terra 承担该职责。

## 协作方式

- 可独立完成的任务应并行分派；存在依赖时按依赖顺序执行。
- 分派前明确每个 Agent 的文件范围、目标和交付物，避免多个 Agent 重复读取或探索相同内容。
- 子 Agent 只向主 Agent 返回精炼结论、关键证据和必要的后续建议；不重复完整过程或无关上下文。
- 主 Agent 负责整合结论、协调任务边界，并在需要时交由 Sol 做最终审查。
