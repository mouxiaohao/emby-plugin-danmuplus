## 1. Isolated r1 baseline and failing coverage

- [x] 1.1 Re-read the isolated r2 worktree seed against the current formal r1 V30 checkout: require the same 13 inherited modified paths and byte inventory digest `fefa366f16020da99af1b8d67863c542c433f2ab9a4be443f82e5d0e9259f2bd`. Record the complete porcelain state under a new `artifacts/2.0.6r2` baseline manifest, classify the already-created `openspec/changes/release-2-0-6-r2-dialog-scroll-containment/` directory as a separate planning-only r2 delta, exclude it from the 13-path seed digest, and record paths/sizes/SHA-256 for those 13 seed files. After writing the manifest, prove the formal r1 checkout, `artifacts/2.0.6r1`, r1 OpenSpec, branch, index, and deployed rollback evidence remain untouched.
- [x] 1.2 Add a failing frontend style contract for shared overlay/card/body scroll containment: require overlay clipping plus containment, card containment, and a shrinkable native-scroll body with vertical containment; cover short content and long body top/middle/bottom states without introducing an entry-specific selector.
- [x] 1.3 Add failing/static guards proving the r2 slice introduces no document/window/global touchmove, pointermove, or wheel listener; no `touch-action:none`, `contain:strict`, host `html/body` lock, host-scroller selector, host scroll read/write, timer, animation-frame restoration, action-sheet delay, or Android/detail-page special branch.
- [x] 1.4 Extend deterministic topology fixtures for a scrollable detail host and a media-library/virtual-list host, all Series/Season/Episode/Movie entry labels, body/header/footer/card/backdrop gesture origins, native body scrolling, post-close host scrolling, and existing secondary-return behavior; document that real CSS scroll chaining remains a required browser/device acceptance gate rather than a fake-DOM claim.

## 2. Shared modal containment and r2 identity

- [x] 2.1 Update only the shared generated Smart Match stylesheet so `.danmuSmartOverlay` adds `overflow:hidden` and `overscroll-behavior-y:contain`, `.danmuSmartCard` adds `overscroll-behavior-y:contain`, and `.danmuSmartBody` adds `flex:1 1 auto`, `min-height:0`, and `overscroll-behavior-y:contain` while retaining its existing padding, `overflow:auto`, layout, sticky summary, mobile safe-area rules, and all horizontal overscroll behavior.
- [x] 2.2 Preserve `closeMenu`, immediate `openDialog`, overlay/card/body construction, Android command ownership, zero-history/zero-Smart-`backbutton` lifecycle, host-pop cleanup, LIFO navigation contexts, trigger-local geometry capture, semantic restoration, candidate order, requests, binding, download, and metadata behavior; verify no r2 input listener or host-state fallback is added.
- [x] 2.3 Advance the formal frontend installation marker from V30 to V31 exactly once, retain mapping protocol V22 exactly, and require V30 plus every Car probe override/marker/badge/trace hook to be absent from the formal r2 source and package.
- [x] 2.4 Stamp independent r2 identity: retain Assembly `2.0.6.0`; set File `2.0.6.2`, Product/informational/configuration `2.0.6r2`, TMDB User-Agent `DanmuPlus/2.0.6r2`, and generated cache token `2-0-6r2`; update exact version fixtures without changing configuration resources or public routes.

## 3. Deterministic and backend verification

- [x] 3.1 Run `node --check` for the formal frontend and regression files plus the complete Smart Match JavaScript regression; prove the new style contract, forbidden-mechanism guards, V31/V22 identity, every existing scroll-return path, Android command behavior, error/cancellation/busy/protected behavior, requests, selection, mapping, binding, and download fixtures pass.
- [x] 3.2 Run the main backend regression and directly relevant R4 identifier, title-fidelity, R3 search-quality, search-term, bounded-search, Episode selection, R4 parent-season context, R5 target-season scope, temporary-range, MGTV, TMDB version, and configuration-resource tests sequentially; preserve all 2.0.6/r1 scoring and provider results.
- [x] 3.3 Run a clean sequential Release solution build; verify Assembly/File/Product identity, cache token, final DLL size/SHA-256, and zero PDB files, CodeView/RSDS, rooted PDB strings, private absolute paths, or diagnostic assets.
- [x] 3.4 Run strict OpenSpec validation, `git diff --check`, changed-file allowlist, candidate-order/V22 guards, credential/private-path scans, and exact before/after r1 seed inventory checks; require cached diff zero and no changes outside the isolated r2 worktree.

## 4. Cumulative documentation and independent r2 package

- [x] 4.1 Update the README current version and cumulative UPDATE with a new 2.0.6r2 section covering modal scroll containment, V31, version identity, and preserved V22/r1 command behavior; retain all prior release headings, demonstration media, and r1 history unchanged.
- [x] 4.2 Create only `artifacts/2.0.6r2` with a verification record and exactly five review-package files: the reviewed r2 DLL, matching V31 JavaScript, stable `SHA256SUMS.txt`, cumulative UPDATE, and VERIFICATION; do not modify, copy over, or re-hash files inside `artifacts/2.0.6r1`.
- [x] 4.3 Re-read package hashes and byte pairing, V31 once/V30 and Car probes zero, V22 once, r2 versions/cache token, five-file inventory, no PDB/CodeView/private path/credentials/temp/log/backup, README media/history preservation, and the immutable r1 inventory.
- [x] 4.4 Have a Sol high-reasoning reviewer inspect the complete r2 diff, CSS scroll-boundary semantics, absence of input/host ownership, entry neutrality, r1 command/scroll-restoration isolation, version/protocol identity, deterministic evidence limits, package safety, rollback plan, and r1 preservation; resolve every blocking finding and repeat affected gates.

## 5. Approval-gated formal deployment and live acceptance

- [x] 5.1 Confirm the user's explicit r2 deployment authorization while preserving separate approval gates for commit, push, merge, tag, PR, release, publication, and cleanup.
- [x] 5.2 Before replacement, re-read the active r1 V30 frontend/DLL/plugin-configuration hashes and marker counts, create a new paired rollback with owner/mode, and verify both that rollback and the earlier r1/D diagnostic rollback sets remain directly usable.
- [x] 5.3 Deploy only the reviewed r2 DLL and V31 frontend pair, restart Emby, require local and external HTTP 200, then read back exact raw/embedded hashes, File/Product/configuration identity, V31 exactly once, V30 and all Car probes zero, V22 unchanged, owner/mode preserved, one Smart Match menu entry, and no diagnostic UI or deployment temporaries.
- [x] 5.4 On desktop, record `CSS.supports("overscroll-behavior-y", "contain")`, the three computed Y-axis containment styles, and the applicable host/dialog scroll positions while testing short content, long-body middle/top/bottom, header/footer/card-margin/backdrop wheel/pan, Series/Season/Episode/Movie detail entries, media-library card control, secondary navigation/return, X/Escape/host Back, and post-close host scrolling. Actual scroll positions, not feature detection alone, are the acceptance gate; require host route, virtual window, order, every underlying scroll position, and horizontal interaction behavior to remain stable while the overlay is connected.
- [ ] 5.5 On both the Android phone and car head unit after force-stop/cold start, record the same feature/computed-style evidence and repeat short/long/top/middle/bottom and header/footer/card-margin/backdrop touch tests for Series whole-show, Season, Season-detail Episode, Movie, and media-library card entry; confirm no background movement, internal body scrolling, safe area, fields/selection/details, child return, top-level close, busy/protected/nested command handling, and ordinary Emby touch/Back after close. Actual scroll-position behavior remains the acceptance gate.
- [ ] 5.6 Inspect bounded redacted client/server diagnostics and configuration/resource state, retain all rollback assets, and immediately restore the exact paired r1 V30 predecessor plus local/external HTTP 200 if any health, hash, scroll, input, route, Back, ordering, or unrelated-behavior criterion fails.

## 6. Final handoff

- [ ] 6.1 Present the final 2.0.6r2 implementation, CSS containment evidence and limitations, complete regression/build/OpenSpec/review results, version/V31/V22 identity, r1 preservation proof, review-package paths and hashes, desktop/phone/car acceptance, deployed/rollback status, and remaining Git/publication gates without exposing credentials, private paths, media identifiers, or raw responses.
