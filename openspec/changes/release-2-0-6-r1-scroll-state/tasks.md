## 1. Baseline and failing characterization

> Revision note (2026-08-18): authorized C/D phone and car-head-unit probes disproved the previously completed Android history/native-`backbutton` contract and established cancelable Emby `command: "back"` ownership. Tasks whose checked implementation or evidence depended on that superseded contract are reopened below; Git history and `VERIFICATION.md` retain the earlier work rather than allowing stale checkboxes to describe the final V30 package.

- [x] 1.1 Before any r1 application-code, version, or documentation edit, record base commit `d22a1069524bd891c5b36c758f75f4112a19e1f4`, full porcelain status, completed 36/36 change state, review/deployed hashes, original-checkout status, and the path, size, and SHA-256 of every existing 2.0.6 modified or untracked file; save the actual tracked binary patch and its hash, classify only the already-created r1 OpenSpec directory as planning delta, tree-hash and keep `artifacts/2.0.6` byte-for-byte immutable, then create or select `codex/release-2.0.6r1-scroll-state` without committing or pushing.
- [x] 1.2 Extend the frontend fake DOM with vertical scroll geometry and add failing real-trigger fixtures proving that Series `查看候选`, parent-title/ProviderId rematch after a busy view, composite remainder `手动匹配`/`重新匹配`, Episode source selection, and Movie part/version selection each enter their secondary page at zero while retaining authoritative row order.
- [x] 1.3 Revise parent-return fixtures so visible buttons and Android command-owned return share the same closure, while preserving preactivation geometry captured before native focus changes the body scroll. Keep the expanded `逐集映射` action/row/section/pre-recorded-neighbor/clamped-raw/zero ladder, successful Season/range structure changes, child rerenders, nested isolation, pre-child failure cases, missing composite target, and Episode/Movie recovery-child return without duplicate or stale contexts.
- [x] 1.4 Add failing desktop-mode fixtures proving dialog open, X, Escape, force close, and ordinary disposal make zero `pushState`, `replaceState`, and `history.back` calls, while a real host `popstate` disposes every stacked overlay without a second traversal or Smart Match parent navigation.
- [x] 1.5 Add explicit Android-identity and command fixtures for secondary return, top-level close, busy/protected retention, nested topmost ownership, no-overlay/non-back passthrough, zero dialog history, and zero Smart Match `backbutton` listeners; retain mixed-case/whitespace positives plus malformed or throwing getter, responsive-width, and touch-only desktop counterexamples.

## 2. Dialog parent-child viewport state

- [x] 2.1 Add per-dialog transient last-in-first-out navigation contexts plus trigger-local two-phase helpers that passively sample parent action/row/section/neighbor geometry on preactivation and atomically consume it at the matching trusted click; reject stale samples for untrusted/programmatic clicks, split legacy Android touch from desktop mouse without timers, reset a completed destination to zero, restore against rebuilt geometry, and clamp only the final numeric fallback.
- [x] 2.2 Assign opaque presentation anchors and action roles backed by existing stable logical identities to Series/Season rows, composite temporary ranges, and Episode/Movie candidate rows without exposing media identifiers; bind pending geometry to the exact trigger and stable action/row/section identity, pass the actual click event, and make all five handlers terminate without render/rebuild/request when capture returns no context.
- [x] 2.3 Reset newly entered `renderSeriesSeasonPicker`, `renderCompositeGroupPicker`, `renderEpisodeSourcePicker`, and `renderMoviePartPicker` pages after their initial content exists, and reset initial/replacement ordinary `renderCandidatePicker` and `renderItemCandidatePicker` results including empty results; retain the same parent context across child search/error rerenders rather than pushing a duplicate.
- [x] 2.4 Route each child's visible return button and Android command-owned return state machine through one return closure; use it for successful Season/range application and parent-return recovery, render current parent data before restoring action-first fallback order, and pop exactly one context. Consume a failed provisional transition only after parent restoration; retain one context through recoverable submission/`preDownloadRecovery`; clear it only after a valid accepted TaskId removes the return path, dialog disposal, host navigation, or explicit abandonment.
- [x] 2.5 Prove inline detail expansion, `setBusy`, parent overview rendering, same-page non-navigation updates, and recoverable submission/progress neither create nor prematurely consume a navigation context; prove accepted terminal progress clears it, direct Season candidate to one-Season composite target gains no fictitious parent return, and no score calculation, candidate filter, provider regrouping, or re-sort is introduced.

## 3. Platform-scoped command ownership and host navigation

- [x] 3.1 Retain one deterministic Android command-environment predicate that trim/case-insensitively accepts `navigator.userAgentData.platform === "Android"` or an `Android` user-agent token, catches throwing getters, defaults malformed or missing signals to desktop, and never uses viewport width, responsive layout, or touch capability.
- [x] 3.2 Freeze `backMode` as only `desktop` or `android-command` when each dialog opens; make both modes issue zero `pushState`, `replaceState`, and `history.back` calls for open, return, explicit close, Escape, force close, and disposal; remove dialog history tokens, guards, ignored-pop counters, and timers.
- [x] 3.3 Install exactly one script-lifecycle document capture listener for exact `command: "back"`; gate it to the topmost connected `android-command` dialog, leave no-overlay/desktop/non-back traffic untouched, and do not register a Smart Match `backbutton` listener in Android command mode.
- [x] 3.4 Implement fail-safe command ownership: if the command is not already canceled, call `preventDefault` before dialog mutation; handle once only after `defaultPrevented` is true, including preexisting cancellation; do not stop propagation, and leave Smart Match unchanged when cancellation is impossible/ineffective/throws; after successful cancellation, retain ownership without history/`backbutton` fallback even when the handler returns false or throws.
- [x] 3.5 Treat every host `popstate` as Emby-owned by disposing the complete connected Smart Match overlay stack and its per-dialog listeners without internal parent return or another traversal, while leaving the script-lifecycle command/popstate singleton listeners installed; leave `closeMenu`, immediate fixed-overlay insertion, trigger-local passive preactivation, desktop X/Escape, and all host-scroll exclusions unchanged.
- [x] 3.6 Export only narrow test hooks for platform mode, transient navigation context, destination reset/restoration, disposal, command dispatch, and real button workflow coverage; formal source/package MUST exclude every Car probe override, marker, badge, trace state, and trace hook and MUST NOT expose identifiers, route data, host scroll, or diagnostics through requests, configuration, or persistence.
- [x] 3.7 Prove source and package contain no r1 `CloseWatcher`, Navigation API interception, dialog history mutation, Smart Match Android `backbutton`, timer/second-fallback command path, `requestAnimationFrame` scroll restoration, host-scroller selector, or responsive/touch platform heuristic.

## 4. Frontend deterministic verification

- [x] 4.1 Extend the fake environment with real capture/bubble command propagation, cancelable/defaultPrevented controls, preexisting cancellation, ineffective/throwing `preventDefault`, handler false/throw, listener counters, host-route state, history counters, dialog scroll geometry, opaque anchor lookup, UA/UA-CH identity, and host `popstate` dispatch without host-scroll or animation machinery.
- [x] 4.2 Run the complete Smart Match JavaScript regression and confirm all four parent/child boundaries plus five trigger entries, pointer/keyboard and legacy platform timing, stale/untrusted/cancelled/detached guards, full semantic fallback ladder, changed-height updates, nested/recovery contexts, terminal cleanup, desktop zero-history/stacked host-pop cleanup, and Android command secondary/top/busy/protected/nested behavior. Also prove no-overlay/non-back passthrough, cancelable/preexisting/ineffective/throwing cancellation, handler false/throw, singleton listener, stable host route, zero history, and zero Smart Match `backbutton` deterministically.
- [x] 4.3 Re-run existing lazy candidate-detail, force-refresh, manual keyword, whole-Series navigation, composite/range mapping, source/version, progress, busy protection, cancellation, error-presentation, request, selection, mapping, binding, and download fixtures to prove command ownership and scroll state alter no unrelated semantics.
- [x] 4.4 Verify the formal source and review-package JavaScript are paired, V30 occurs exactly once, V29 and every Car probe marker/override/badge/trace hook occur zero times, candidate order remains server-authored, and mapping protocol assertions remain V22.

## 5. Complete 2.0.6r1 identity and documentation

- [x] 5.1 Keep assembly version `2.0.6.0`, set file version `2.0.6.1`, and set informational, configuration, TMDB User-Agent, and user-visible pairing text to `2.0.6r1` without changing plugin/page identity.
- [x] 5.2 Advance the frontend installation marker from V29 to V30, retain mapping protocol V22 and configuration cache token `2-0-6r1`, and leave Assembly/File/Informational/Configuration/TMDB User-Agent identities at `2.0.6.0`/`2.0.6.1`/`2.0.6r1`.
- [x] 5.3 Update the cumulative 2.0.6r1 `UPDATE.md` entry and README current behavior for zero dialog history, Android command ownership, semantic parent return, and V30 while preserving prior release history, installation notes, screenshots, demonstration images, and compatibility statements.
- [x] 5.4 Revise both `artifacts/2.0.6r1/VERIFICATION.md` copies with the C command-trace and D phone/car-head-unit evidence, explicitly state that probes establish design but do not accept formal V30, add final command/history/marker/package slots, and retain all redaction/privacy exclusions.

## 6. Full local verification and review package

- [x] 6.1 Re-run the main backend regression and directly relevant R4 identifier, title-fidelity, candidate-detail, search/navigation, cancellation, and configuration assertions sequentially; preserve every 2.0.6 scoring and selection result.
- [x] 6.2 Run a clean sequential Release solution build, verify assembly/file/product versions, confirm no PDB or private CodeView/absolute-path disclosure, and record the final DLL size and SHA-256.
- [x] 6.3 Run strict OpenSpec validation, final change status, `git diff --check`, changed-file allowlist review, README/history/media preservation checks, original-checkout verification, and credential-safe source/artifact/package scans that never print secret values.
- [x] 6.4 Reassemble and re-read a local 2.0.6r1 review package containing only the verified DLL, matching V30 JavaScript asset, stable checksums, cumulative notes, and verification record; exclude all Car probe assets/code, build trees, logs, backups, credentials, private helpers, and unrelated artifacts.
- [x] 6.5 Have a Sol high-reasoning reviewer inspect the final diff, command ownership/cancellation order, singleton/topmost gating, zero-history/backbutton invariants, host-pop cleanup, parent/child viewport behavior, diagnostic exclusion, ordering isolation, version/protocol identity, test evidence, documentation preservation, and package safety; resolve every blocking finding and repeat affected checks.

## 7. Approval-gated Emby browser verification

- [x] 7.1 Keep the reviewed formal V30 package local and use the user's explicit authorization before Synology replacement, Emby restart, and authenticated live acceptance; do not infer authorization to push, merge, tag, publish, or create a PR.
- [x] 7.2 Before replacing the active D diagnostic asset, back up the paired deployed DLL, plugin configuration, and frontend configuration with hashes, owner, and mode; retain and verify directly usable formal-r1 and diagnostic rollback sets.
- [ ] 7.3 Deploy only the reviewed formal V30 pair, restart and health-check Emby, then verify exact deployed hashes, V30 exactly once, V29 and every Car probe marker/override/badge/trace hook zero, one Smart Match menu item, and no diagnostic UI on desktop, phone, or car head unit.
- [ ] 7.4 Confirm live that each Series view/rematch, composite range manual/rematch, Episode source, and Movie version child opens at the top and returns by every applicable button/Android command path. Recheck expanded-mapping action restoration, surviving row/section/neighbor/raw/zero fallbacks, changed-height/removed-range behavior, click, touch/long-press, and safe area while preserving server order.
- [ ] 7.5 Confirm desktop dialog open/X/Escape and browser Back remain host-stable with zero dialog history; on both phone and car head unit confirm command-owned child return, top-level close, busy/protected retention, nested topmost handling, and unchanged host route, then confirm physical Back with no overlay remains Emby-owned. Verify unrelated requests, details, controls, ordering, binding, download, and metadata behavior remain unchanged.
- [ ] 7.6 Inspect bounded redacted logs and configuration/resource state, retain rollback assets, and immediately restore the verified paired predecessor plus health-check Emby if any formal acceptance criterion fails.

## 8. Final handoff

- [ ] 8.1 Present the final 2.0.6r1 V30 implementation, all secondary-entry and semantic parent-return boundaries, Android command ownership evidence, zero-history/host-pop behavior, version/V30/V22 identity, artifact paths and hashes, formal phone/car-head-unit acceptance, deployment and rollback status, and remaining Git/publication gates without exposing credentials or private paths.
