## 1. 冻结 2.0.7 基线与失败契约

- [x] 1.1 记录当前隔离 worktree 的分支/HEAD/porcelain、完整 2.0.7 changed-file 清单、最终 reviewed DLL/V32 哈希、OpenSpec 71/71 状态和当前部署读回证据；把这些既有修改视为 2.0.7r1 的不可丢失基线，不重写 `artifacts/2.0.7` 或既有验证记录。

> Baseline record (2026-08-19): branch `codex/release-2.0.7-remainder-auto-matching`, HEAD `07fbb408d54ee1b6201d4f217122079070527c5a`, porcelain 18 tracked modified / 25 untracked / 0 staged, parent change 71/71. The retained reviewed 2.0.7 pair is DLL `5598B4D3B33A7124AD6811D2CDAE5E14884A20347923B4F563914596FC97E752` and V32 JavaScript `40860D2EE2039F2DF123CF0FAFDBDD2D5A3706BBDDB668241395F7AAFE42FDCC`; its existing live readback record and `artifacts/2.0.7` remain unchanged.
- [x] 1.2 在任何生产代码修改前新增失败回归：Season 有 displayed/eligible 统计但四项 ignored count 全零时，真实范围摘要仍错误包含 `忽略项不可选择，也不会进入下载。`；同时冻结有忽略项时现有明细与提示正例。

> Red-test record (2026-08-19): before any production JavaScript edit, the full frontend regression exited `1` at the new real composite-render assertion: `a real composite render with displayed/eligible scope but zero ignored counts must not show the ignored-item safety notice`. The preceding positive render assertion proved the existing ignored breakdown and exactly-one notice behavior remained intact.
- [x] 1.3 冻结实现 allowlist 为前端脚本/回归、版本配置与版本断言、README/UPDATE、新 change 和 `artifacts/2.0.7r1`；明确排除 Controller/DTO、Season ownership、planner、下载、Provider、持久化和 mapping protocol 行为文件。

> Slice allowlist (2026-08-19): only the frontend asset/regression, project/configuration/TMDB version files and assertions, README/UPDATE, this change, and the future `artifacts/2.0.7r1` are permitted. Controller/DTO/Season ownership/planner/download/Provider/persistence/V22 behavior files and all parent artifacts are excluded.

## 2. 权威忽略计数与条件提示

- [x] 2.1 增加一个只读前端归一化 helper，仅从四个服务器 ignored count 字段及其 Pascal/camel casing 读取计数，把缺失、null、非数字、NaN/Infinity、负数和零归零，把有限正数取非负整数后求和；不得解析本地化文本、DOM 或 displayed-minus-eligible 差值。
- [x] 2.2 让 `scopeSummaryLine` 在构造 `只读忽略 N 集（分类）` 的同一字符串分支内一次性附加固定后缀 `忽略项不可选择，也不会进入下载。`；三个 renderer 只消费该摘要，不保留独立 `scopePresentationLine`/第二 ignored-total gate；零 ignored 时明细与提示均不存在。
- [x] 2.3 增加真实渲染回归，分别覆盖四类单项 ignored、混合 ignored、全零、字段缺失、Pascal/camel casing、非法/负数/Infinity、小数取整，以及 whole-Series 两季一有一无时逐 Season 隔离。
- [x] 2.4 增加 positive-to-zero rematch/rebuild 重渲染回归，证明旧提示不会残留或重复；保持 single-Season 与 whole-Series 共用路径结果一致。
- [x] 2.5 扩展安全回归，证明显示条件变化不把 ignored Episode 渲染为可选组、不写入 composite selections/request、不进入权威 mapping/download；后端现有四项计数和所有执行门禁保持字节级实现不变。

> Superseded partial green record (2026-08-19): the first frontend pass covered composite rendering but did not cover non-composite whole-Series/single-Season notice presentation and allowed JavaScript coercion of booleans/arrays. Tasks 2.1-2.4 are reopened pending explicit red-to-green P1 regressions; the request-level ignored S00 execution fence under task 2.5 remains unchanged.

> P1 red-test record (2026-08-19): before the P1 production repair, the complete frontend regression exited `1` with one aggregated contract failure naming non-composite whole-Series positive notice, whole-Series positive-to-zero replacement, single-Season-without-CompositePlan positive notice, single-Season positive-to-zero replacement, and rejection of boolean/array/numeric-string-array coercion. The same fixture also covers false, object, empty/whitespace string rejection and retains finite positive number/numeric-string flooring.

> Superseded P1 green record (2026-08-19): after adding an ignored-only strict numeric normalizer and a pure shared scope-presentation helper, production/test syntax and the complete frontend regression exited `0`. Composite, non-composite whole-Series, and single-Season-without-CompositePlan shared that response-only breakdown/notice rule; both non-composite paths cleared positive notices on zero rerender, booleans/arrays/objects/empty strings normalized to zero, and finite positive numbers/non-empty numeric strings were floored. Strict r1 validation and `git diff --check` also exited `0`. The later single-string-branch requirement invalidated the separate helper structure and this record is historical only.

> Single-branch red/green record (2026-08-19): before changing production JavaScript, the new aggregated contract made the complete frontend regression exit `1`, explicitly identifying the separate presentation helper, non-identical composite/non-composite whole-Series/direct single-Season rendering, and positive/zero/category/mixed branch violations. After moving the fixed safety suffix into the positive `scopeSummaryLine` branch, deleting `scopePresentationLine`, and making all three renderers consume only that summary, production syntax, regression syntax, and the complete frontend regression exited `0/0/0`. The notice literal occurs once in production source, the removed helper has zero references, and all three real renderer consumers are covered. Strict normalization, positive-to-zero cleanup, whole-Series isolation, zero/illegal-count safety, and ignored selection/request/mapping/download fences remain green. Strict r1 validation and `git diff --check` also exited `0`; tasks 4.1-4.6 stay unchecked pending a fresh clean build, full release matrix, replacement package, and Sol final review.

## 3. 2.0.7r1 身份与累计文档

- [x] 3.1 保持 AssemblyVersion `2.0.7.0`，设置 FileVersion `2.0.7.1`，将 InformationalVersion、PluginConfiguration.Version 和 TMDB User-Agent 更新为 `2.0.7r1` / `DanmuPlus/2.0.7r1`，同步主回归和 TMDB alias 版本断言。
- [x] 3.2 将前端正式安装标记由 V32 升至 V33，确认 V33 恰好一次、V32 及更旧正式标记为零，配置缓存 token 为 `2-0-7r1`，mapping protocol 在前后端继续保持 V22。
- [x] 3.3 在 `UPDATE.md` 顶部增加累计 2.0.7r1 条目，并把 README 当前版本/提示行为/版本配对更新为 2.0.7r1；保留全部历史记录、安装信息、演示图片及其引用，不改写 2.0.7 的既有说明。

> Identity record (2026-08-19): the serial Release main regression passed with Assembly `2.0.7.0`, File `2.0.7.1`, informational/configuration `2.0.7r1`, cache `2-0-7r1`, and TMDB User-Agent `DanmuPlus/2.0.7r1`. The frontend regression passed with exactly one formal V33 flag, zero V32/older formal flags, and V22 unchanged. README adds a new current r1 section and UPDATE prepends a cumulative r1 entry; the existing 2.0.7 and older history remains intact.

## 4. 本地验证、审阅包与最终审查

> Single-branch invalidation record (2026-08-19): the user required the notice to be an inseparable suffix of the `只读忽略 N 集（分类）` branch returned by `scopeSummaryLine`, rather than a second `scopePresentationLine` ignored-total gate. The reviewed JavaScript `A3DF9F78B19FF122F50559B518A9B3947AE1455AD8C319A23090E1E81921242B`, its five-file review package, and the isolated remote staging copy are superseded and MUST NOT be deployed. A separately created, verified root-only current-2.0.7 backup remains retained and restorable; no candidate replacement or restart occurred, and the active instance remains the reviewed 2.0.7 DLL/V32/V22 pair. Tasks 4.1-4.6 are reopened for a fresh red-to-green, release matrix, package rebuild, and final review.

- [x] 4.1 顺序运行前端两个 Node 语法检查与完整 Smart Match 回归，确认有/无忽略项、rematch 清理、whole-Series 隔离、V33/V22 及既有候选/映射/下载行为全部通过。

> Pre-fix non-authoritative record (2026-08-19): production JavaScript syntax, regression JavaScript syntax, and the then-current Smart Match frontend regression each exited `0` after the clean Release build. Sol subsequently found uncovered non-composite positive-ignore rendering and coercive boolean/array normalization P1 gaps, so this evidence must be replaced by a complete post-fix rerun.

> Post-P1 frontend release record (2026-08-19): after a fresh clean Release build, production JavaScript syntax, regression JavaScript syntax, and the complete Smart Match frontend regression exited `0/0/0`. The suite includes composite/non-composite whole-Series/direct single-Season positive and zero notice paths, positive-to-zero replacement, strict ignored-count types, V33/V22, and existing candidate/mapping/download behavior.

> Single-branch fresh frontend record (2026-08-19): after the mandatory single-string-branch correction and a new clean Release build, production JavaScript syntax, regression JavaScript syntax, and the complete Smart Match frontend regression exited `0/0/0`. The suite covers the exact single returned summary string, all three real renderer entrances, each/mixed/zero/invalid authoritative ignored count, positive-to-zero cleanup, whole-Series isolation, V33/V22, and the existing zero-selection/request/mapping/download fences. No pre-correction frontend result was reused.
- [x] 4.2 顺序运行主后端回归、目标季范围/Season 0/复合 planner/下载 stale fence 等相关独立套件，证明本次前端改动没有改变服务器 ignored counts、eligible inventory 或执行范围。

> Pre-fix non-authoritative record (2026-08-19): the main full Release regression, its composite-season-planner mode, and all 12 independently enumerated RegressionTests projects passed sequentially. This matrix must be rerun after the P1 repairs before release evidence is accepted.

> Post-P1 backend release record (2026-08-19): after the fresh clean build and frontend pass, the main full Release regression, composite-season-planner mode, and all 12 independently enumerated RegressionTests projects were rebuilt/restored and passed sequentially. No pre-P1 test result was reused.

> Single-branch fresh backend record (2026-08-19): the main full Release regression and `--composite-season-planner` each exited `0`; all 12 independently enumerated projects were then restored and run sequentially with `restore=0/run=0`, including R207 core/orchestration, R5 target-season scope, MGTV, bounded/episode/search/temporary/title policies, and R3/R4 suites. No product assertion failed and no earlier binary or test result was reused.
- [x] 4.3 执行无竞争 clean Release build，核对 DLL `2.0.7.0`/`2.0.7.1`/`2.0.7r1`、V33 asset、V22 和 cache token，记录大小与 SHA-256。

> Pre-fix non-authoritative build record (2026-08-19): clean/restore/Release build exited `0/0/0` with 131 pre-existing warnings and zero errors. The resulting DLL (`535CA3649E36459D7A1A0990F31191A6A8621F304222ED0DB248B3396C058F42`) and JavaScript (`7FB8FFF55EFE85814F2850C03D2EB7CCB57B6628CE61F2EBBBA682E41E3FEA88`) were invalidated by the later P1 findings and are not review/package/deployment inputs; a new clean build and complete identity audit are required after repair.

> Post-P1 clean-build record (2026-08-19): a new clean/restore/Release build exited `0/0/0` with 131 pre-existing warnings and zero errors. The freshly rebuilt DLL is 1,725,952 bytes, SHA-256 `535CA3649E36459D7A1A0990F31191A6A8621F304222ED0DB248B3396C058F42`; the repaired V33 JavaScript is 251,956 bytes, SHA-256 `A3DF9F78B19FF122F50559B518A9B3947AE1455AD8C319A23090E1E81921242B`. Assembly/File/Product are `2.0.7.0`/`2.0.7.1`/`2.0.7r1`; config/UA are r1, cache is `2-0-7r1`, V33/V32 are `1/0`, the notice literal occurs once, and frontend/backend mapping protocol remains V22.

> Single-branch fresh build record (2026-08-19): with no competing `dotnet` process, clean/forced restore/Release build exited `0/0/0` with 131 pre-existing warnings and zero errors. The resulting DLL is 1,725,952 bytes, SHA-256 `535CA3649E36459D7A1A0990F31191A6A8621F304222ED0DB248B3396C058F42`; the corrected source JavaScript is 251,708 bytes, SHA-256 `43A10697A33CF590D7CFA9B2A27832E2142DB7D7D1028C6DFB82734F97CB01A2`. Assembly/File/Product are `2.0.7.0`/`2.0.7.1`/`2.0.7r1`, configuration/User-Agent/cache are r1, V33/V32/older formal flags are `1/0/0`, frontend/backend mapping protocol remains V22, the notice literal occurs once, `scopePresentationLine` occurs zero times, and exactly three renderers directly consume `scopeSummaryLine`.
- [x] 4.4 运行本 change、2.0.7 parent change 和 `fix-sparse-episode-number-alignment` 的严格 OpenSpec 验证、最终 status/coherence、`git diff --check`、changed-file allowlist、历史/图片保留、PDB/私有路径/凭据/Authorization/部署端点审计。

> Audit record (2026-08-19): strict validation and status/show coherence passed for r1, the 71/71 parent, and fix-sparse (27/28 with only its separately gated live item open); `git diff --check` exited `0`. The r1 tool/edit trail is confined to the frozen frontend/version/assertion/docs/change allowlist, while the retained 2.0.7 artifacts still match their reviewed hashes. Mono.Cecil comparison of the reviewed 2.0.7 DLL and fresh r1 DLL found 6,255 methods with zero additions/removals and only three expected version/cache/UA body differences (`PluginConfiguration` initialization, generated configuration page names, TMDB UA use); 863 managed type shapes were identical, four expected constants changed, and only the generated config-page resource changed. Controller, Model, Season planning, planner, download, and Provider matching bodies had zero method differences. README/UPDATE retained every baseline heading, all three HTML demonstration-image references, and installation/configuration sections. Release output contained zero PDB files, RSDS records, private PDB/absolute paths, private endpoints, credential material, Authorization material, or Bearer material; three generic `.pdb` identifiers and credential field-name identifiers contained no path or secret. Source scanning classified two credential-like assignments as regression placeholders and zero as production/documentation material.

> Single-branch fresh audit record (2026-08-19): strict validation and status/show coherence passed again for r1, the 71/71 parent, and fix-sparse at 27/28 with only its separate live gate open; `git diff --check` exited `0`. The r1 edit trail remains inside the frozen frontend/version/assertion/documentation/change/package allowlist. Mono.Cecil comparison of reviewed 2.0.7 and the fresh r1 DLL found 6,255 methods on each side, zero additions/removals, only the three expected configuration/config-page/TMDB User-Agent body changes, 863 identical type shapes, four expected constants, and one generated configuration-page resource change. Controller, Model, Season-planning, planner, download, and Provider scopes each have zero changed method bodies. README/UPDATE retain all 17/28 baseline headings, all three image references, and installation/configuration sections. Primary Release output has zero PDB/RSDS/private path/private endpoint/credential/Authorization/Bearer material; three generic `.pdb` identifiers contain no path. Changed release text has two deterministic regression credential placeholders and zero production/documentation credentials, Authorization/Bearer material, private endpoints, private local paths, or private NAS paths.
- [x] 4.5 组装仅本地的 `artifacts/2.0.7r1/review-package`，仅包含 reviewed DLL、匹配 V33 JavaScript、UPDATE、SHA256SUMS 和 VERIFICATION；验证 manifest、版本和禁项，不自动 push、merge、tag、发布、归档或部署。

> Historical superseded package record (2026-08-19): the initial `artifacts/2.0.7r1/review-package` candidate contained exactly the five required files. Its DLL (1,725,952 bytes, `535CA3649E36459D7A1A0990F31191A6A8621F304222ED0DB248B3396C058F42`) was byte-identical to the fresh Release output; its V33 JavaScript (251,956 bytes, `A3DF9F78B19FF122F50559B518A9B3947AE1455AD8C319A23090E1E81921242B`) and UPDATE (`F0259A97ED0A0F0064B5CD9E7C40F3A660A09D1518B086375C724DDA8E89CF06`) were byte-identical to their then-reviewed sources. Its manifest contained exactly those three payload entries and all hashes matched. Package scanning found zero PDB files, RSDS/private PDB or absolute paths, private deployment endpoints, credential material, Authorization values, or Bearer credential shapes; three generic `.pdb` identifiers contained no path. This candidate is historical and obsolete: it was superseded first by the single-branch JavaScript correction and later by the final documentation refresh, and MUST NOT be treated as the current package. At the time of this record, no push, merge, tag, publication, archive, or deployment had been performed.

> Historical superseded single-branch package record (2026-08-19): the in-place rebuilt review-package candidate contained exactly the five required files. Its DLL (1,725,952 bytes, `535CA3649E36459D7A1A0990F31191A6A8621F304222ED0DB248B3396C058F42`) was byte-identical to the fresh Release output; its corrected V33 JavaScript (251,708 bytes, `43A10697A33CF590D7CFA9B2A27832E2142DB7D7D1028C6DFB82734F97CB01A2`) and then-current UPDATE (`F0259A97ED0A0F0064B5CD9E7C40F3A660A09D1518B086375C724DDA8E89CF06`) were byte-identical to their reviewed sources. Its manifest had exactly those three names and all three hashes matched. The obsolete `A3DF...` hash occurred zero times in payloads/manifest and once only in VERIFICATION's historical invalidation record. Package scanning found zero PDB/RSDS/private path/private endpoint/credential/Authorization/Bearer material; three generic `.pdb` identifiers contained no path. This candidate is historical and obsolete because the later documentation refresh replaced its UPDATE payload and manifest entry; it MUST NOT be described as the current package. At the time of this record, no push, merge, tag, publication, archive, or deployment had been performed.

> Final documentation-refresh package record (2026-08-19): the current `artifacts/2.0.7r1/review-package` contains exactly five files. Its reviewed DLL is 1,725,952 bytes with SHA-256 `535CA3649E36459D7A1A0990F31191A6A8621F304222ED0DB248B3396C058F42`; its corrected V33 JavaScript is 251,708 bytes with SHA-256 `43A10697A33CF590D7CFA9B2A27832E2142DB7D7D1028C6DFB82734F97CB01A2`; and its final documentation-refreshed UPDATE is 27,521 bytes with SHA-256 `A714E8B898D69133FF97F3FC9EBDB282C1F0824732825E16AF997A7D9E0DC917`. The current 261-byte `SHA256SUMS.txt` has SHA-256 `BFE9469F6A25878F933A1039162CE54BFF92A28DBC0F48D6DE2358B6E9297551`, lists exactly `Emby.Plugin.Danmu.dll`, `DanmuSmartMatch.CustomCssJS.js`, and `UPDATE.md`, and matches all three payloads. The current VERIFICATION record has SHA-256 `AA76AF7B6AD1B3D3169F4F10343223E7B8B159B8553B8B93087F62DBA39A3B14`. This final record supersedes both historical package candidates above; no package payload is changed by this evidence correction.
- [x] 4.6 由 Sol high 最终审查提示门禁、权威字段/非法值归一化、重渲染无残留、ignored 零选择/零下载、版本/V33/V22、范围审计、验证证据和包安全，并关闭所有 P0/P1。

> Superseded final-review record (2026-08-19): Sol high completed the final review of the now-invalidated `A3DF...` JavaScript after its P1 repair and release matrix. The later user-mandated single-string-branch structure was outside that review, so task 4.6 is reopened and this record is historical evidence only.

> Single-branch final-review record (2026-08-19): Sol high reviewed the rebuilt fixed-suffix package pairing the fresh DLL `535CA3649E36459D7A1A0990F31191A6A8621F304222ED0DB248B3396C058F42` with the corrected V33 JavaScript `43A10697A33CF590D7CFA9B2A27832E2142DB7D7D1028C6DFB82734F97CB01A2` and returned PASS with no remaining P0/P1 finding. The authoritative four-count normalization, indivisible `scopeSummaryLine` suffix branch, three renderer consumers, positive-to-zero cleanup, ignored zero-selection/zero-download fences, version/V33/V22 identity, scope/IL evidence, and five-file package safety were accepted. This review does not perform or imply live replacement, preview, publication, or any Git release action.

## 5. 另行批准的现场步骤

- [x] 5.1 在任何 live preview、Synology 替换、Emby 重启或部署前取得用户明确批准；若未批准，记录 deferred/no-op 并保持当前已验收 2.0.7 运行实例不变。

> Authorization record (2026-08-19): the user explicitly authorized this controlled 2.0.7r1 deployment and read-only live preview validation in the current thread. Connection and authentication material remains transient and is not recorded in repository evidence.
- [x] 5.2 若获批，先成对备份并校验当前 2.0.7 DLL/V32/CustomCssJS 配置/Danmu 配置及 owner/mode，再原子替换 reviewed 2.0.7r1 DLL/V33 配对、重启并读回 health/version/cache marker/protocol/hash/config/log；失败时自动恢复 2.0.7。

> Controlled deployment record (2026-08-19): a verified root-only paired 2.0.7 DLL/V32/CustomCssJS/Danmu backup was created before replacement. The first replacement restarted healthy but a local post-metadata verifier incorrectly required the new DLL/XML sizes to equal the old sizes; its trap restored and revalidated the complete old HTTP-200/V32/V22 state. After synthetic positive and uid/mode/size-negative tests corrected that verifier, a fresh same-process precheck, atomic replacement, restart, and independent closing readback passed: DLL `535CA3649E36459D7A1A0990F31191A6A8621F304222ED0DB248B3396C058F42`, decoded normalized-LF JavaScript `459DEC015075C85354F858C5B597FBF53F53098780846EEC2678C05B92D53D5C`, V33/V32/V22 `1/0/1`, plugin/configuration `2.0.7.0`/`2.0.7r1`, cache `2-0-7r1`, unchanged Danmu config `02519AFD92022BABACF9E6D516C44C0DDE0117A2744593501D8CB29222536069`, retained owner/mode, HTTP 200, and latest-startup Danmu load/error `1/0`. Verified backup and corrected staging sets remain root-only and restorable.
- [x] 5.3 仅用只读 MatchPreview 验证一个 ignored total 大于零的 Season 显示一次提示、一个 ignored total 为零的 Season 不显示提示，并确认 ignored 项没有选择控件或下载映射；除非另有授权，不得确认、下载、绑定、导入、启动后台任务或写 metadata。

> Live preview record (2026-08-19): the first positive/zero observations came from an already-open Chrome tab that still held the predeployment in-memory script; its zero result still showed the sentence and is explicitly non-authoritative cache evidence. After a complete reload activated V33, `妄想学生会` returned two whole-Series Seasons from 26 local Episodes: displayed/eligible/S00-ignored were `21/13/8` and `23/13/10`, each positive summary contained its immediately adjacent fixed suffix exactly once, each exposed 13 temporary-group Episodes, and no ignored Episode had a selection control. The zero-ignore `葬送的芙莉莲` result was displayed/eligible `38/38`, contained neither `只读忽略` nor the safety sentence, and retained eligible groups of 28 and 10 Episodes. Live actions were limited to MatchPreview, dialog close, and full reload; no confirm, download, binding, import, background task, metadata write, composite submission, or download payload occurred.
- [x] 5.4 报告本地与现场状态、回滚材料、最终哈希和仍未获批的 Git/publication gates；不得泄露凭据、token、地址或私有部署路径。

> Final state record (2026-08-19): the closing no-preview readback reconfirmed HTTP 200, the reviewed DLL/V33/V22 pair, unchanged configuration, retained owner/mode, plugin/config/cache identities, one clean startup load, and checksum-valid root-only rollback/staging material. Transient authentication values were cleared and SSH was closed. No endpoint, credential, token, item identifier, private local path, backup path, or staging path is recorded. Push, merge, tag, release/publication, and OpenSpec archive remain unapproved and unperformed.
