## 1. Source metadata contract

- [x] 1.1 Add an additive provider-neutral SourceMetadata model and thread it through discovery candidates, exact identifier decisions, authoritative season segments, and collection reconstruction without changing persisted provider identifiers; cover automatic, manual, supplementary-segment, and direct temporary binding entry points, and retain a cloned server-owned selected-candidate snapshot for safe field-level fallback.
- [x] 1.2 Extend provider detail adapters so exact Episode and Movie identifier resolution returns available parent source title, trustworthy work year, and category through detail lookup only; never substitute provider publication/upload timestamps for `Year`, keep Movie parent identity separate from optional `PartTitle`/leaf identity, define Episode `SourceTitle` as the upstream parent media/season title, and preserve successful downloads when optional metadata is unavailable.
- [x] 1.3 Update controller preview/reconstruction mapping so exact-ID result payloads and selected temporary collections expose structured source metadata without fuzzy discovery; merge non-empty exact-detail title/category first, prefer a trustworthy exact work year over the server-owned candidate snapshot, fill a missing/untrustworthy year from that snapshot, and ignore browser metadata.
- [x] 1.4 Add a Movie-only verified-downloadable-part contract and short-lived evidence registry binding scoped to item/provider/parent candidate/token; resolve default/manual downloads to the registered usable leaf and reject stale, cross-scope, explicitly excluded, unregistered, or tampered part identities without changing persisted binding schema.

## 2. Temporary-season presentation

- [x] 2.1 Carry selected candidate metadata through authoritative segment planning into returned collections for automatic matching, manual selection, supplementary-segment selection, and direct temporary binding, including the server-owned candidate snapshot fallback that preserves a trustworthy search work year when detail omits it or exposes only publication/upload time.
- [x] 2.2 Update CustomCssJS collection cards and Movie candidate headings to display localized provider, parent source title, available source year/category, and a distinct optional `PartTitle`, while preserving hidden internal identities and existing range/action behavior.
- [x] 2.3 Add a preview/post-binding Movie-only part selector when more than one de-duplicated verified independently downloadable usable option remains after explicit non-main filtering; default to the first stable-order part without affecting automatic parent-Movie binding, omit a redundant selector for zero/one option, submit only opaque scoped choice evidence, and prove Season/Episode UI is unchanged.

## 3. Bilibili discovery and title evidence

- [x] 3.1 Add bounded typed `media_ft` and `media_bangumi` Bilibili retrieval using compatible session/header handling, merge it with aggregate results by canonical usable parent identity, preserve mapper metadata, keep Movie candidates parent-scoped instead of replacing them with the first `ep_id`, and expose one shared candidates-plus-diagnostics result contract (or equivalent shared channel) consumed by both Movie and Season search paths.
- [x] 3.2 Isolate typed-page failures into diagnostics while retaining previously collected Bilibili and other-provider candidates for both Movie and Season consumers; enforce bounded pagination/record limits and cancellation/timeout behavior.
- [x] 3.3 Implement generic loose and fidelity title normalization for primary titles, aliases, and applicable parent-media/season titles on both local and source paths: retain punctuation-folded loose recall/base scoring; build fidelity forms with Unicode NFKC plus case/whitespace normalization while preserving arbitrary punctuation/symbol type, count, and order; keep one-character and symbol-only suffixes; add exact fidelity matches as positive unique-highest evidence without penalizing mismatches; and retain manual handling when no unique highest candidate exists.
- [x] 3.4 Enumerate Movie leaves only through provider detail adapters that prove stable independent download identity: support Bilibili `ep_id`, gate Tencent on independent `vid`, gate Youku on a stable exact/download version identity, and apply the same threshold to Mgtv/Dandan/Iqiyi; classify with authoritative flags/types/sections first and conservative title fallback, remove explicitly identified non-main units before first selection, retain otherwise indistinguishable usable parts for compatible first-part auto-binding and optional selection, and de-duplicate by verified leaf identity.

## 4. Deterministic regression coverage

- [x] 4.1 Extend C# resolver/model tests for exact Episode and Movie source metadata, Episode parent media/season `SourceTitle`, Movie parent identity plus distinct `PartTitle`, absent optional metadata, exact-without-candidate work year remaining null, trustworthy exact production year winning conflicts, other-provider production-year preservation, and no fuzzy lookup side effect.
- [x] 4.2 Add C# collection-planner and frontend regression coverage proving automatic, manual, supplementary-segment, and direct temporary bindings preserve source title/work year through segment-to-collection reconstruction; trustworthy exact production year wins while server candidate year (including the live-equivalent Dandan 2014 case) fills a missing/untrustworthy detail year; browser metadata is ignored; rendering is consistent and internal identities remain redacted.
- [x] 4.3 Add Bilibili API/mapper/search-engine fixtures proving aggregate-plus-typed merge returns deduplicated parent Movie candidates for Bourne 1–5, parent identity survives leaf resolution, Bilibili publication/upload timestamps never populate work `Year` (including a 2002 Bourne candidate versus 2023 detail publication), the shared result reaches both Movie and Season paths, and typed-page failure preserves successful candidates while surfacing diagnostics.
- [x] 4.4 Add parameterized deterministic scorer tests covering NFKC compatibility equivalence (including `＊`/`*`), unchanged loose punctuation folding, preservation and distinction of arbitrary symbol type/count/order (including `!`, `!!`, and unknown combinations), one-character and symbol-only suffix survival, fidelity mismatch as non-penalizing, fidelity evidence making `妄想学生会＊` uniquely highest over its marker-less sibling, identical behavior through primary-title, alias, and parent-media/season-title paths, and a genuinely equal same-provider pair remaining ambiguous without source-order selection.
- [x] 4.5 Add deterministic provider/evidence/UI fixtures for Movie part selection: usable feature plus explicit trailer/preview/behind-the-scenes/special/clip/interview/bonus/making-of; all remaining units indistinguishable (automatic first-part binding succeeds and selector lists the rest); exactly one usable part; several usable parts with stable order/de-duplication; explicit filtering before default-first selection; part ambiguity never entering Movie candidate ambiguity; Bilibili/Tencent/Youku eligibility gates and other-provider fallbacks; valid non-default download; stale/cross-scope/tampered/explicitly-excluded part rejection; raw-ID redaction; and unchanged Season/Episode flows.

## 5. Verification, packaging, and deployment

- [x] 5.1 Run the complete deterministic .NET and CustomCssJS regression suites plus a Release build after the revised metadata/year-semantics/part-selection work; record commands and results.
- [x] 5.2 Back up r9 deployment assets and live-test exact Episode/Movie metadata, candidate-snapshot year fallback, representative bound temporary collections, Bilibili `谍影重重` 1–5, a Movie with multiple usable Bilibili parts (automatic default and optional manual leaf), exclusion of explicit non-main units before the default, and `妄想学生会＊`; do not use a live title for genuine-tie verification because task 4.4 covers it deterministically. Live inventory contained no explicitly classified non-main Movie unit, so the deterministic task 4.5 fixture is the accepted evidence for that boundary and the residual live-fixture gap is recorded in `VERIFICATION.md` without claiming a live filtering pass.
- [x] 5.3 Package `2.0.3r10` DLL/frontend/source/checksum assets, update version and cumulative release history, publish the release for review, and record hashes plus r9 rollback instructions.
