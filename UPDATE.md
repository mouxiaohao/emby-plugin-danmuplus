# 更新日志

## 2.0.7r1（2026-08-19）

- 修复季度只要存在 displayed/eligible 范围摘要就无条件显示“忽略项不可选择，也不会进入下载。”的问题。前端只读取服务端四项权威 ignored count，由同一个 `scopeSummaryLine` 分支生成 `只读忽略 N 集（分类）` 并直接追加固定后缀“忽略项不可选择，也不会进入下载。”，不保留独立的第二 ignored-total gate；归一化总数大于零时整条分支恰好出现一次，总数为零时仍显示正常范围摘要但不生成该分支。
- 缺失、null、非数字、NaN/Infinity、负数和零均按零处理，有限正数向下取整；Pascal/camel JSON casing、四类单项、混合计数、整剧逐季隔离，以及正数到零的重新匹配/重建均使用当前响应无状态重渲染，不解析本地化文本、DOM 或 displayed-minus-eligible 差值。
- 本次不改变后端 Season ownership、ignored count 生产、可选虚拟组、权威映射、下载过滤、Provider、持久化或 DTO。忽略 Episode 仍不能进入选择、composite request、mapping 或下载，映射协议保持 V22。
- 插件程序集版本保持 `2.0.7.0`，文件版本为 `2.0.7.1`，产品与配置版本为 `2.0.7r1`，TMDB User-Agent 为 `DanmuPlus/2.0.7r1`；前端安装标记由 V32 升至 V33，配置缓存 token 为 `2-0-7r1`。

## 2.0.7（2026-08-19）

- 用户主动单季（包括显式 S00）或整剧中的正季在首段权威成功后，可对最大余集区间进行有界自动递归：先取唯一连续 `Part`/“部分”，再取唯一同年份同集数的相似标题，最后以余集首集年份为准，按父剧名 60、下一逻辑季 20、年份 20 分寻找达到 90 分的正式下一季。Part 成功不增加逻辑季号，逻辑季成功才递增一次。
- 首段权威来源的 Provider 会被锁定为整条余集递归链；初始 canonical 候选、后续逻辑季搜索、详情解析、Part 适用性和年份/集数唯一性均先限定在该 Provider 内。跨 Provider 的重复或合并来源不参与唯一性/歧义判断，同站没有可信结果时静默停止且不跨站回退；每轮只有在锁定 Provider 的搜索与详情覆盖完整时才进行唯一性判断，覆盖不完整时按 `Unknown` 安全停止且不降级到后续规则。
- Part 标记只提供序号，不能制造标题族身份：上一段存在非父剧标题核心时，后续候选必须真实共享该核心；只有逻辑第 1 季且首段所有权威标题通道均没有非父剧核心时，才允许父剧名直出的兼容兜底。现场反馈后的修正确保 `星尘斗士` 只接同 Provider 的 `埃及篇`，不会被父剧别名桥接到 `石之海 Part.2`/`Part.3`；`石之海` 仅按同 Provider 的连续 Part 处理。
- 余集决策始终使用完整 canonical 候选及服务端验证的来源 Episode/CommentId 清单；已用稳定来源、明确冲突季号、详情不完整及验证后不超过 3 集的来源均会排除。Part 缺号、歧义、取消、超时、Provider/详情失败、过期证据与无进展均安全终止，不会降级到低优先级规则。
- 首段已成功后，余集停止保留已映射前缀和普通临时未匹配区间，不显示顶层匹配失败。唯一同年份的 partless 候选在验证来源集数不一致时仍可绑定，但仅由服务端重建的 response-only `EpisodeCountMismatchWarning` 显示一次黄色“本地与来源集数不一致”提示；重建或重新匹配不会保留旧提示。
- 每个自动余集选择继续走 `ExplicitAnchor`、完整选段、来源清单、目标绑定 evidence、generation 与 SHA-256 fingerprint 的权威重建链；warning、决策层级、比较年份/集数、稳定来源和余集范围变化均会触发 stale fence。映射协议保持 V22，不增加浏览器可作者化字段。
- 用户主动 whole-Series 会递归处理其中的正季，并仅在搜索前跳过 S00；仅无人值守/background 入口在搜索前跳过 S00 且绝不递归，媒体库导入、item-added、自动下载、retry/replay 保持既有首段行为。插件程序集/文件版本为 `2.0.7.0`，产品与配置版本为 `2.0.7`，TMDB User-Agent 为 `DanmuPlus/2.0.7`；前端安装标记由 V31 升至 V32，配置缓存 token 为 `2-0-7`，映射协议保持 V22。

## 2.0.6r2（2026-08-18）

- 为智能匹配共用浮层增加 `overflow:hidden` 与纵向 overscroll containment，卡片增加同一纵向边界；正文增加 `flex:1 1 auto`、`min-height:0` 与纵向 containment，同时保留原有 padding、`overflow:auto`、粘性摘要、移动端安全区和横向交互，使长内容继续由正文原生滚动。
- Series、Season、Episode、Movie 从详情页、季度/单集行或媒体库卡片进入时复用同一三层 CSS。修复不增加 document/window/global 触摸、指针或滚轮监听，不使用 `touch-action:none`、`contain:strict`、宿主 `html/body` 锁、宿主滚动查找/读写、计时器、动画帧恢复、action-sheet 延迟或 Android/详情页分支。
- 保留 2.0.6r1 的同步菜单关闭/弹窗创建、零 dialog history、Android `command: back` 所有权、无 Smart Match `backbutton`、宿主 `popstate` 清理、后进先出父子页逻辑视口恢复、候选顺序、请求、绑定、下载、元数据和 V22 映射协议。
- 确定性 fake-DOM 回归只证明样式契约、入口中立性与既有生命周期不变；浏览器和真实设备对短内容、正文中部及顶部/底部边界的原生滚动链行为仍是部署后的独立验收门禁。
- 插件程序集版本保持 `2.0.6.0`，文件版本为 `2.0.6.2`，产品与配置版本为 `2.0.6r2`，TMDB User-Agent 为 `DanmuPlus/2.0.6r2`；前端安装标记由 V30 升至 V31，配置缓存 token 为 `2-0-6r2`，映射协议保持 V22。

## 2.0.6r1（2026-08-18）

- 修复智能匹配复用滚动容器时二级页继承父页位置的问题：整剧季度候选、复合季度临时区间、单集来源、电影版本，以及直接/重新搜索的普通候选结果均在内容生成后同步回到顶部，不改变服务端候选顺序、筛选、评分或分组。
- 返回父页时保存对话框内的瞬时逻辑视口：按原操作、原行、所属季度/区间、预先记录的相邻行依次恢复，全部逻辑锚点消失后才把旧数值位置约束到新滚动范围。成功匹配造成高度变化、“逐集映射”折叠、区间合并/移除、嵌套来源/版本选择和可恢复提交失败均保留正确的后进先出语义。
- 导航触发改为局部两阶段采样：鼠标/触摸与 Enter/Space 在浏览器默认焦点滚动前被动记录几何，匹配的可信点击才原子创建一次返回上下文；旧浏览器按 Android 触摸/桌面鼠标分流，避免合成鼠标覆盖触摸位置。不可信或程序化点击拒绝旧采样并使用点击时几何；取消、长按菜单、拖动、失焦、候选变化、未点击或已脱离 DOM 均不留状态，且已脱离入口不会继续渲染、重建或请求。
- 修复同路由 `pushState` 在桌面 Chrome 重建虚拟列表、在 Android 手机和车机刷新当前页面并吞掉弹窗的问题。所有平台的弹窗打开、内部返回、X、`Escape`、强制或普通关闭均不再修改/遍历会话历史；宿主 `popstate` 由 Emby 接管并一次清理所有智能匹配浮层。
- Android/WebView 仅在 UA-CH platform 或 UA 明确标识 Android、且存在顶层智能匹配弹窗时接管 Emby 的精确 `command: back`：先成功取消宿主命令，再执行一次二级返回、顶层关闭或忙碌保护；不注册智能匹配自己的原生 `backbutton` 监听，也不使用 history 或第二回退通道。没有弹窗时系统返回仍由 Emby 正常处理。
- 不捕获或恢复 Emby 宿主滚动，不使用延时帧、`CloseWatcher` 或 Navigation API；后端评分、提供商、DTO、下载、标识符和 V22 映射协议保持 2.0.6 行为。
- 插件程序集版本保持 `2.0.6.0`，文件版本为 `2.0.6.1`，产品与配置版本为 `2.0.6r1`，TMDB User-Agent 为 `DanmuPlus/2.0.6r1`；前端安装标记由 V29 升至 V30，映射协议保持 V22。

## 2.0.6（2026-08-18）

- 季度评分中的父剧名 60 分与季名 20 分使用等权、单位成本的规范化 Levenshtein 连续相似度；完全一致为满分，完全不相干为 0，中间差异按 `1 - 编辑距离 / 最大长度` 比例计分。年份仍为精确 0/20，集数仍不计分。
- 保留父剧名精确包含时的满分与精确残差分解；没有精确父剧名时，完整来源标题可与当前允许的父标题候选连续比较，但不会删除推测的模糊片段，也不会跨来源标题通道叠加证据。
- 显式季号先校验冲突，再只移除已确认正确的通用季号并比较描述残差；不删除“之”或其他词汇。`第2季：西行` 对 `唐朝诡事录之西行` 的季名相似度为 `0.6667`，总分为 `0.9333`；`西行` 对 `东行` 为 `0.5000`，总分 `0.9000`。
- 普通自动门槛保持包含边界的 `>= 0.90`，TMDB 别名门槛保持 `>= 0.80`；没有季名分最低门槛、冲突上限或自动 veto。Movie、候选资格、保真证据、搜索顺序和共享 Jaro-Winkler 消费者不变。
- 插件程序集/文件版本为 `2.0.6.0`，产品与配置版本为 `2.0.6`，TMDB User-Agent 为 `DanmuPlus/2.0.6`；前端安装标记保持 V28，映射协议保持 V22。

## 2.0.5r1（2026-08-18）

- l1-l2：删除过时的复合季度警告，并把“仅用于本次下载映射”的说明收敛为每个适用结果一次；权威来源集数多于库内集数时仅显示一次黄色提示，不改变评分、映射或下载资格。
- l3、l7：单站失败只形成有界诊断，其他已完成站点仍可正常评分与自动处理；全部站点失败、明确取消、歧义或结构不完整继续关闭式失败。共享搜索不再施加单站 10 秒、交互 30 秒和自动 45 秒截止时间，但保留明确取消、并发门、非协作任务结算、站点自身传输保护及 Movie/Episode 180 秒下载边界。
- l4-l5：配置页标题改为 `DanmuPlus 配置`，源码链接指向本项目 `main` 分支；程序集、插件标识、配置路由、已保存设置和其他更新/发布地址不变。
- l6：TMDB 动画别名中的临时请求错误不会中断后续别名；只有达到当前 80 分别名阈值的结果才替换候选，全部别名耗尽后隐藏别名候选与 TMDB 内部诊断，并可用权威父剧名重新匹配。父标题证据在当前别名与原始父剧名间取最大值。
- l8-l9：普通评分不再使用季度/年份冲突分数上限，也不再把 0.85 等低于普通自动阈值的符号保真候选抬升到阈值；保真证据仍可解决真正同分。严格的“双空残余”完整标题 NFKC 符号兜底只补足同一标题对的季名分，不恢复置信度加分。
- l10：显式手动关键词保留站点清洗、候选资格、合并去重、服务端普通评分/原因及按站点公平的 60 条展示窗；不调用 TMDB、不按自动阈值删行、不预选，也不在用户显式选择并通过权威校验前下载、绑定或写入元数据。
- 季度普通评分最终为父剧名 60 分、季名 20 分、年份精确吻合 20 分、集数 0 分。集数差异只在权威映射后以黄色提示呈现；TMDB 短父别名的受限终止季标记恢复仍只在别名父标题最大值路径内生效。
- 临时映射错误会显示可操作原因；整剧预览遇到瞬时空 Season 只自动重试一次。失败、取消、过期或零写入计划继续禁止标识符和元数据写入。
- 稀疏集号映射升级为 V22/V28：显式可靠编号默认按零偏移对齐，用户显式本地/来源锚点按编号差继续；编号不可靠时对整个窗口使用稳定位置兜底，不在窗口内混用模式。精确来源集 ID 是权威锚点，计划指纹覆盖对齐意图、窗口、来源顺序与精确映射，stale-write fence 在下载或元数据写入前拒绝过期计划。
- Spy Family S3 的稀疏库内 E1-E6/E10-E13 保持 E10→来源 E10；Frieren 显式 E29→来源 E1 时，缺少 E30 后 E31→来源 E3。逐集重试与七日重放冻结本地集、来源集和 CommentId 元组；显式单季仍可处理 Season 0，整剧与无人值守/媒体导入在搜索前跳过 Season 0。
- 插件程序集/文件版本为 `2.0.5.1`，产品与配置版本为 `2.0.5r1`，TMDB User-Agent 为 `DanmuPlus/2.0.5r1`；前端安装标记为 V28，映射协议为 V22。完整回归、clean Release、严格 OpenSpec、diff/范围/凭据/包清单审计及 Sol-high 终审均通过；群晖备份、成对部署、哈希回读、Emby 进程/HTTP 200/插件加载和只读预览验收均通过，未触发下载、绑定或强制刷新，旧 XML 未自动清理。

## 2.0.4r2（2026-08-15）

- 动画弹弹 Play 匹配可按最多三个中文别名、英文标题、日文标题的确定顺序使用 TMDB 备用标题；达到 80 分即停止，候选按实际别名评分，显式手动关键词始终优先。
- 补强库内来源信息与 Bilibili 类型化检索：电影合并受限的 `media_ft`/`media_bangumi`，Series/Season/Episode 只使用 `media_bangumi`；超时型临时错误有限重试，明确取消立即传播，降级结果不缓存。
- 增加七日跳过结果的本次任务重放入口，同时保持已有标识符、来源元数据、下载和 XML 安全边界。该版本最终基线为 `f8a4356`，已推送 `develop`；当时未合并 `main`、未发布正式 Release。

## 2.0.3r10（2026-08-14）

- 精确电影和单集标识符通过对应网站详情返回来源作品标题、可信发行/首播年份与类型；单集展示父作品或季度标题，不执行模糊搜索。上传、网页发布时间和 Bilibili `pub_time`/`pubdate` 不再冒充作品年份。
- 自动、手动、补充分段和直接临时绑定统一保留服务端来源元数据；详情缺字段时只从服务端候选快照逐字段回填，修复弹弹 Play 2014 年候选在临时季度重建后丢失年份的问题，忽略浏览器提交的元数据。
- Bilibili 搜索合并受限的 `media_ft` 与 `media_bangumi` 分类结果，按父媒体身份去重并保留分页失败诊断；`谍影重重` 1–5 均可作为电影候选，普通视频结果不会混入。
- 标题比较增加通用的 Unicode NFKC 符号保真证据，在维持标点宽松召回的同时区分符号类型、数量和顺序；`妄想学生会＊` 可唯一命中带星号候选，真实同分仍保持手动处理。
- 电影候选保持父电影身份；网站能证明多个独立可下载正片时，默认使用稳定顺序第一项并提供仅限电影的版本选择。明确的预告、花絮、特别篇、片段、访谈和制作特辑会在默认首选前过滤，选项由短期服务端证据校验且不公开原始 leaf ID。
- 插件文件版本为 `2.0.3.10`，产品版本为 `2.0.3r10`；前端安装标记保持 V23，映射协议保持 V21。
- 完整 .NET/Node 回归、Release 构建、严格 OpenSpec 校验和 Emby 4.9.5.0 现场验收已通过。现场 39 部 Bilibili PGC 电影没有可触达的明确非正片样本，因此该过滤边界以确定性回归为验收证据，不声明现场过滤通过。

## 2.0.3r9（2026-08-13）

- 季度匹配升级为显式映射与复合季度规划，支持重新匹配、受限搜索、候选质量排序、部分季度映射和父季度感知的映射界面。
- 普通季度仅处理 `ParentIndexNumber` 与目标季度一致的单集；整剧匹配不再把 Season 0 当作普通目标。用户仍可从真实 Season 0 条目单独匹配特别篇。
- 候选详情改为点击“解析并查看详情”后按需加载；强制刷新状态、临时季度手动匹配、来源标题展示和错误呈现保持一致。
- 修复芒果 TV 搜索接口兼容性，并将异常限制在单一提供商，不影响其他已启用弹幕源。
- 手动自定义关键词仅用于提供商发现：跨语言别名、空格、字面量 `+`、标点及非 ASCII 查询返回的有效季度候选不再被二次标题相似度过滤；自动入库仍保留严格标题证据规则。
- 插件文件版本为 `2.0.3.9`，产品版本为 `2.0.3r9`；前端安装标记为 V23，映射协议为 V21。
- 已通过后端、前端、季度范围、搜索质量、芒果 TV 和 r9 窄增量回归，并在 Emby 4.9.5.0 上完成部署与只读验收。r5 隔离写入 fixture 因 Emby 未建立索引而未完成，未将该项标记为通过。

## 2.0.3r8（2026-08-13）

- 将已失效的芒果 TV 搜索请求迁移到已验证的 PC suggest 接口，并把新响应规范化为现有候选模型。
- 芒果 TV 的禁止访问、限流、畸形响应或合法空结果只形成该提供商的诊断，其他已启用提供商继续完成。
- 保持 r7 的 V23 前端、V21 映射协议、季度范围、按需详情、临时季度、下载和持久化行为不变。

## 2.0.3r7（2026-08-13）

- 统一临时季度的“重新匹配”和“手动匹配”入口，候选分数只显示一次，并为每个候选提供按需“解析并查看详情”。
- 已确认的临时季度映射同时显示本地单集标题和已验证的来源单集标题，不暴露内部标识符。
- 阻塞请求期间隐藏“强制刷新”，仅在可执行下载的页面恢复；保留 r5/r6 的范围、证据、确认和下载语义。

## 2.0.3r6（2026-08-13）

- 单集、整剧逐季度和直接季度的手动候选详情改为显式点击后加载，初始候选列表不再提前请求来源详情。
- 对候选详情增加短期证据令牌、目标绑定和输入校验；查看详情不会触发绑定、下载、元数据写入或复合规划。
- 将一个对话框级“强制刷新”复选框移到左下角，在真正开始下载时才锁定快照；继续沿用 r5 的目标季度范围规则。

## 2.0.3r5（2026-08-13）

- 整剧智能匹配只枚举已知的正数季度，不再把 Season 0 作为普通目标季度。
- 普通季度仅允许 `ParentIndexNumber` 等于目标季度号的单集进入预览、临时分组、下载、重试和自动处理；混入的 S00、其他季度及未知父季度单集只读显示为已忽略。
- 用户直接打开真实 Season 0 时，仍可仅使用其自身 Parent 0 单集完成匹配。
- r5 的隔离写入 fixture 因 Emby 在限定等待期内未建立测试条目而未执行写路径验收；测试库、条目和精确路径按记录清理，任务保持未完成，未虚构通过结论。

## 2.0.3r4（2026-08-12）

- 引入父季度感知的规划上下文、目标协调和计划代际，使整剧与单季度入口使用一致的单集归属信息。
- 前端 V20 显示权威映射分数、来源和逐集映射，隐藏 ProviderId、MediaId、来源单集 ID 与证据令牌等内部字段，并改进窄屏映射详情布局。
- 已部署候选在只读预览中验证了普通季度映射和当时的 S00 临时分组；后者在 r5 被新的严格季度范围规则取代。

## 2.0.3r3（2026-08-11）

- 收紧搜索词为承载身份的标题，过滤明显无关候选，并按提供商公平、稳定地投影结果；提供商失败形成不完整诊断而不阻塞其他来源。
- 支持部分季度显式映射与确认：已确认范围可单独下载，剩余单集保持未匹配，不把不完整结果持久化为完整季度绑定。
- 增加候选证据、搜索质量和部分季度回归；V19 前端保留服务器给出的分数与来源。

## 2.0.3r2（2026-08-11）

- 增加整剧/季度分组的移除、恢复和范围内重新匹配，并为临时范围建立稳定的替换映射。
- 引入有界搜索：单提供商调用 10 秒、交互操作 30 秒、自动操作 45 秒，全局并发 3、单站并发 1；取消操作可提前终止等待。
- 默认搜索只发送有限的身份标题，显式关键词每个提供商只调用一次；失败提供商不触发自动绑定或下载。

## 2.0.3r1（2026-08-11）

- 建立复合季度显式映射：一个本地季度可由多个来源季度和单集组成，并以稳定连续分组显示映射与未匹配范围。
- 下载、重试和重启恢复使用精确来源单集身份；仅在成功持久化后更新对应单集元数据，并保持 Emby 原有季度成员关系和外部元数据标识不变。
- 增加复合季度状态、提供商策略、规划器、前端交互和完整回归；配套 V17 DLL/JavaScript 已在 Emby 4.9.3.0 验证。

## 2.0.2r4（2026-08-11）

- 动态标题片段与同站别名搜索统一覆盖整剧内季度、单独季度、单集所属季度和电影；电影仍使用各网站的电影专用搜索与类型过滤。
- 别名轮次的季度/单集评分调整为标题 35%、年份 20%、集数 45%，电影为标题 70%、年份 30%；标题相关度至少 0.72，总置信阈值仍为 0.90，完整标题搜索保持原权重。
- 同一媒体存在多个有效弹幕网站标识符时，严格按已启用网站的配置顺序验证；同站内单集优先自身 Episode ID，再回退所属季度 ID。
- 修复季度标识符与 Series 标识符值相同时被错误忽略的问题；季度仍只认自身标识符，不重新启用 Series 回退。
- 修复“重新智能匹配”未运行自动别名发现的问题：未修改默认标题时不发送自定义 `keyword`，用户编辑后仍保持单一关键词搜索语义。
- 季度首次成功落盘后保存选中网站的季度 ID，每个成功集保存自己的 Episode ID；同时精确清除该媒体上其他已注册弹幕网站的普通标识符，包括停用网站。
- Manual、TMDB、TVDB、IMDb 和其他非插件字段保持不变；失败、跳过、取消、超时、过时代际或未落盘结果不触发标识符清理。
- Android/WebView 回退支持二级界面返回上级、顶层关闭；匹配搜索进行中消费系统回退但保留右上角 X，结果显示后恢复正常回退。
- Android 窄屏标题栏加入状态栏安全区域，下移右上角 X；弹窗遮罩仍不会关闭界面。
- 前端安装标记升级为 `__embyDanmuSmartMenuV15`。

## 2.0.2r3（2026-08-11）

- 电影只读取自身标识符；季度只读取自身标识符；单集优先读取本集标识符，失败后才使用所属季度标识符。Series 上的插件标识符不再参与子媒体匹配、下载、重试或自动入库。
- 无可用本地标识符时继续进入手动绑定或全站智能评分；“重新智能匹配”显式绕过本地标识符和保存绑定。
- 标准标题搜索不足时追加有限的标点标题片段与同站别名搜索，不使用片名专用字典。
- 弹弹 Play 独立季度过滤非正片后按稳定列表顺序映射为本季 `1..N`，同时保留真实 AnimeId/EpisodeId。
- 整季第一条有效 XML 成功保存后写入季度标识符；每个成功集写入真实单集标识符。
- 哔哩哔哩限定 PGC：季度保存 `season_id`，电影和单集保存 `ep_id`，`aid,cid` 只在下载时解析。
- Emby 元数据编辑器补充哔哩哔哩和芒果 TV 标识符字段。
- 前端安装标记升级为 `__embyDanmuSmartMenuV14`。

## 2.0.2r2（2026-08-11）

- 本地外部标识符命中后只请求该站点同一精确 ID 的详情，不再为补全展示字段执行关键词搜索、评分或跨站竞争。
- 标题、年份、分类和声明集数只有在网站详情明确返回时显示；没有返回的字段保持“未知”，不以 Emby 本地信息伪造。
- 芒果 TV、腾讯和优酷在精确详情可可靠取得可用剧集列表时，使用实际列表数量显示集数。
- 搜索、评分和“重新智能匹配”继续使用原全站流程，与精确标识符详情严格分离。
- 前端安装标记升级为 `__embyDanmuSmartMenuV13`。

## 2.0.2r1（2026-08-11）

- 智能匹配来源和决策代码改为中文；未知代码显示中文兜底，原始值只保留在诊断文字中。
- 点击遮罩不再关闭智能匹配窗口，移除底部取消按钮；普通界面只通过右上角 X 或 `Escape` 关闭。
- 保持 2.0.1-r6 的后端候选、下载、重试、ProviderId 写回和自动入库行为。
- 前端安装标记升级为 `__embyDanmuSmartMenuV12`。

## 2.0.1-r6（未单独发布）

- 匹配预览由后端返回 `matchOrigin`、`decisionReason` 与完整候选，网页端不再计算评分、重排或推断最终匹配。
- 本地外部标识符命中显示为匹配成功，并提供“重新智能匹配”入口。
- 重新智能匹配发送 `mode=rematch`、`rematch=true` 和兼容旧端的 `force=true`。

## 2.0.1-r5（2026-08-10）

- 上一正式 Release 基线；完成服务端统一评分、站点优先级决策与前端候选展示的基础整合。

## 2.0.1-r4（2026-08-10）

- 移除当时发行包中的共享测试 Worker 地址，代理地址由管理员自行选择或部署。
- 新安装默认使用自定义 API 模式，代理 CORS 配置保持为空。

## 2.0.1-r3（2026-08-10）

- 弹弹 Play 配置增加互斥的代理 API 与自定义 API 模式。
- 代理模式支持兼容 `cf_worker.js` 的 CORS 前缀；自定义模式继续直连官方 API并本地签名。
- 两种模式均使用 `/search/anime`、`/bangumi` 和 `/comment`，不使用视频 Hash 或 `/match`。

## 2.0.1-r2（2026-08-10）

- 智能匹配菜单扩展到电视剧、季度、单集和电影的详情页、卡片及 Android 长按菜单。
- 增加电影跨站评分与跟踪下载；单集支持候选来源集数建议和手动覆盖。
- 单目标进度统一支持停止、超时和重试，并修复爱奇艺电影 ID 解析与腾讯请求超时。

## 2.0.1-r1（2026-08-10）

- 为所有来源增加统一 XML 1.0 字符过滤，保留合法中文、换行、字符引用、emoji 等 Unicode 字符。
- 爱奇艺和哔哩哔哩原始 XML 首次解析失败时，在清理非法字符后重试。
- 取消以 1 KB 为唯一有效性标准，改为按弹幕条目和序列化结果判断。

## 2.0.0（2026-08-09）

- 建立独立 DanmuPlus 增强线，统一多站搜索、季度评分、非正片过滤、分段重试、部分 XML、后台任务和独立 CustomCssJS 前端。
# 2.0.4r2（2026-08-14）

- TMDB 动画辅助搜索按简体短标题、简体完整标题、繁体短标题、繁体完整标题稳定排序；先排除已包含在库标题中的重复片段，再保留最多三个中文关键词。
- 中文关键词未达到 80 分时，按需使用 TMDB 英文主标题和日文原始/本地主标题继续搜索，不使用英日别名，并继续按本次搜索词独立评分。
- 自动季度匹配遇到明确的季度号冲突或相差两年以上的已知年份时，将候选限制在 80 分以下，避免错误季度阻止别名搜索；手动关键词、电影、Season 0 和精确绑定不受影响。
- 整剧与单季度匹配窗口改为展示服务器返回的库内剧集信息；单集和电影展示保持原样。
- 批量下载完成后，可仅对本次因 7 天重复策略跳过的集数执行“忽略跳过再次下载”；范围完全由服务器任务状态确定，不会重下本次已成功的集数。
