# 更新日志

## 2.0.2r4（2026-08-11）

- 动态标题片段与同站别名搜索统一覆盖整剧内季度、单独季度、单集所属季度和电影；电影仍使用各网站的电影专用搜索与类型过滤。
- 别名轮次的季度/单集评分调整为标题 35%、年份 20%、集数 45%，电影为标题 70%、年份 30%；标题相关度至少 0.72，总置信阈值仍为 0.90，完整标题搜索保持原权重。
- 同一媒体存在多个有效弹幕网站标识符时，严格按已启用网站的配置顺序验证；同站内单集优先自身 Episode ID，再回退所属季度 ID。
- 修复季度标识符与 Series 标识符值相同时被错误忽略的问题；季度仍只认自身标识符，不重新启用 Series 回退。
- 修复“重新智能匹配”未运行自动别名发现的问题：未修改默认标题时不发送自定义 `keyword`，用户编辑后仍保持单一关键词搜索语义。
- 季度首次成功落盘后保存选中网站的季度 ID，每个成功集保存自己的 Episode ID；同时精确清除该媒体上其他已注册弹幕网站的普通标识符，包括停用网站。
- Manual、TMDB、TVDB、IMDb 和其他非插件字段保持不变；失败、跳过、取消、超时、过时代际或未落盘结果不触发标识符清理。
- Android/WebView 回退支持二级界面返回上级、顶层关闭；匹配搜索进行中消费系统回退但保留右上角 X，结果显示后恢复正常回退。
- Android 窄屏标题栏加入状态栏安全区域，下移右上角 X；弹窗遮罩仍不会关闭界面。
- 前端安装标记升级为 `__embyDanmuSmartMenuV15`。

## 2.0.2r3（2026-08-11）

- 电影只读取自身标识符；季度只读取自身标识符；单集优先读取本集标识符，失败后才使用所属季度标识符。Series 上的插件标识符不再参与子媒体匹配、下载、重试或自动入库。
- 无可用本地标识符时继续进入手动绑定或全站智能评分；“重新智能匹配”显式绕过本地标识符和保存绑定。
- 标准标题搜索不足时追加有限的标点标题片段与同站别名搜索，不使用片名专用字典。
- 弹弹 Play 独立季度过滤非正片后按稳定列表顺序映射为本季 `1..N`，同时保留真实 AnimeId/EpisodeId。
- 整季第一条有效 XML 成功保存后写入季度标识符；每个成功集写入真实单集标识符。
- 哔哩哔哩限定 PGC：季度保存 `season_id`，电影和单集保存 `ep_id`，`aid,cid` 只在下载时解析。
- Emby 元数据编辑器补充哔哩哔哩和芒果 TV 标识符字段。
- 前端安装标记升级为 `__embyDanmuSmartMenuV14`。

## 2.0.2r2（2026-08-11）

- 本地外部标识符命中后只请求该站点同一精确 ID 的详情，不再为补全展示字段执行关键词搜索、评分或跨站竞争。
- 标题、年份、分类和声明集数只有在网站详情明确返回时显示；没有返回的字段保持“未知”，不以 Emby 本地信息伪造。
- 芒果 TV、腾讯和优酷在精确详情可可靠取得可用剧集列表时，使用实际列表数量显示集数。
- 搜索、评分和“重新智能匹配”继续使用原全站流程，与精确标识符详情严格分离。
- 前端安装标记升级为 `__embyDanmuSmartMenuV13`。

## 2.0.2r1（2026-08-11）

- 智能匹配来源和决策代码改为中文；未知代码显示中文兜底，原始值只保留在诊断文字中。
- 点击遮罩不再关闭智能匹配窗口，移除底部取消按钮；普通界面只通过右上角 X 或 `Escape` 关闭。
- 保持 2.0.1-r6 的后端候选、下载、重试、ProviderId 写回和自动入库行为。
- 前端安装标记升级为 `__embyDanmuSmartMenuV12`。

## 2.0.1-r6（未单独发布）

- 匹配预览由后端返回 `matchOrigin`、`decisionReason` 与完整候选，网页端不再计算评分、重排或推断最终匹配。
- 本地外部标识符命中显示为匹配成功，并提供“重新智能匹配”入口。
- 重新智能匹配发送 `mode=rematch`、`rematch=true` 和兼容旧端的 `force=true`。

## 2.0.1-r5（2026-08-10）

- 上一正式 Release 基线；完成服务端统一评分、站点优先级决策与前端候选展示的基础整合。

## 2.0.1-r4（2026-08-10）

- 移除当时发行包中的共享测试 Worker 地址，代理地址由管理员自行选择或部署。
- 新安装默认使用自定义 API 模式，代理 CORS 配置保持为空。

## 2.0.1-r3（2026-08-10）

- 弹弹 Play 配置增加互斥的代理 API 与自定义 API 模式。
- 代理模式支持兼容 `cf_worker.js` 的 CORS 前缀；自定义模式继续直连官方 API并本地签名。
- 两种模式均使用 `/search/anime`、`/bangumi` 和 `/comment`，不使用视频 Hash 或 `/match`。

## 2.0.1-r2（2026-08-10）

- 智能匹配菜单扩展到电视剧、季度、单集和电影的详情页、卡片及 Android 长按菜单。
- 增加电影跨站评分与跟踪下载；单集支持候选来源集数建议和手动覆盖。
- 单目标进度统一支持停止、超时和重试，并修复爱奇艺电影 ID 解析与腾讯请求超时。

## 2.0.1-r1（2026-08-10）

- 为所有来源增加统一 XML 1.0 字符过滤，保留合法中文、换行、字符引用、emoji 等 Unicode 字符。
- 爱奇艺和哔哩哔哩原始 XML 首次解析失败时，在清理非法字符后重试。
- 取消以 1 KB 为唯一有效性标准，改为按弹幕条目和序列化结果判断。

## 2.0.0（2026-08-09）

- 建立独立 DanmuPlus 增强线，统一多站搜索、季度评分、非正片过滤、分段重试、部分 XML、后台任务和独立 CustomCssJS 前端。
